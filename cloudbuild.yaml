steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-f', 'Dockerfile.static', '-t', 'gcr.io/tryandfit/mirrify-multi', '.']
  # Push the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/tryandfit/mirrify-multi']
images:
  - 'gcr.io/tryandfit/mirrify-multi'

